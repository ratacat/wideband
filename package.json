{
  "name": "wideband",
  "version": "0.1.0",
  "description": "Fan-out search across agentic search providers, merged into deduplicated sources with provenance and cost telemetry",
  "license": "MIT",
  "type": "module",
  "private": true,
  "module": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "wideband": "src/cli/main.ts"
  },
  "scripts": {
    "test": "bun test",
    "cli": "bun src/cli/main.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
